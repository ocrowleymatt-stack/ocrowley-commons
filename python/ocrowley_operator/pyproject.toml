[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ocrowley_operator"
version = "0.1.0"
description = "Digsbody draft/review operator (execute off by default)"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["ocrowley_operator*"]
