[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ocrowley_planner"
version = "0.1.0"
description = "Issue → plan heuristics (scope, deps, risk)"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["ocrowley_planner*"]
