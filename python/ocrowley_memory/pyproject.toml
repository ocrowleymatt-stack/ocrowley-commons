[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ocrowley_memory"
version = "0.1.0"
description = "Morpheus memory: TTL store, context, retrieval, audit"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["ocrowley_memory*"]
