[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ocrowley_agents"
version = "0.1.0"
description = "Mnemosyne multi-agent registry, dispatcher, coordinator"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["ocrowley_agents*"]
