[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ocrowley_policy"
version = "0.1.0"
description = "Unified risk levels, no-go rules, approval gates"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["ocrowley_policy*"]
